<div class="container"> 
 <div class="row">
   
   <%= render 'books/list' %>
   
  <div class="col-md-8 offset-md-1">  


<h2>Users</h2>

<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
    </tr>
  </thead>
    
<% @users.each do |user| %>
  <tbody>
  <tr>
    <td><%= attachment_image_tag user, :profile_image, size: '40x40' , format: 'jpeg', fallback: "no_image.jpg" %></tb>
    <td><%= user.name %></tb>
    <td><%= link_to "Show", user_path(user) %></tb>
  </tbody>
  </tr> 
<% end %>
</table>

  </div>
 </div>
</div>